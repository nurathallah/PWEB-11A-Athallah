       //Operator tambahan(+)
        var a = 30;
        var b = 15;

        var jumlah = a + b;
        console.log(jumlah);
        console.info("Operator tambahan");

        //Operator Kurang (-)
        var a = 10;
        var b = 15;

        var kurang = a - b;
        console.log(kurang);
        console.info("Operasi Kurang");

        //Operator Perkalian (*)
        var a = 10;
        var b = 15;

        var Perkalian = a * b;
        console.log(Perkalian);
        console.info("Operator Perkalian");
        
        //Operator Pembagian (/)
        var a = 10;
        var b = 15;

        var Bagi = a / b;
        console.log(Bagi);
        console.info("Operator Bagi");
        
        //Operator Modulus (%)
        var a = 15;
        var b = 4;

        var modulus = a % b;
        console.log(modulus);
        console.info("Operator Modulus");
        
        //Operasi Kurung () -> mendahului Operasi aritmetika
        var a = 4;
        var b = 5;
        var c = 6;

        var nilai = a + (b-c);
        var nilaiAkhir = 10 + (2-5)*2;
        var nilaiAkhir1 = 5 % 2 + ( 3 + 5 ) * 2 * 4;
        console.log(nilaiAkhir1);
        console.log(nilaiAkhir);
        console.log(nilai);
        console.info("Operator Kurung");
        
        //1. Konversi ke String
        let angka = 100;
        let infauzanmarried = false;
        let floatNumber = 3.14;

        //String();
        const angkaToString = String




        //2. Konversi ke number
        let StringNumber = "100";
        let StringFloat = "3.14";
        let isFulanAbsen = "false";

        //Number param -> Variabel atau ekspresi yang akan dikonversi 
        const stringNUmberToNUMBER = Number(stringNumber);
        const stringFloatToNumber = Number(stringFloat);
        const isFulanAbsenToNumber = Number(isFulanAbsen);

        // .parseInt(param)
        let pixel = "100px";
        let height = "170cm";
        let weight = "60kg";   